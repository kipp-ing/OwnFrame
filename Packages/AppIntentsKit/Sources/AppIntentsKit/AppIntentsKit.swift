// AppIntentsKit — the framework-free intent logic layer (spec 800): registry, command
// service, state snapshot, and error taxonomy over the HAControlKit control protocols.
// Deliberately no `import AppIntents` anywhere in this package (FR-800-09/SC-800-05);
// the AppIntent/AppEntity shells live in the app target. Types land via T003–T005.
